pipeline {
   agent any

   stages {

      stage('install req') {
        steps{
          sh 'python3 -m venv env'
          sh 'env/bin/pip install -r requirements.txt'
        }
        }
      stage('run tests'){
        steps{
            sh 'pytest tests.py'
        }
      }

  }
}
